return {
  view = {
    side = "right",
    width = 50,
  },
  -- actions = {
  --   open_file = {
  --     resize_window = true,
  --   },
  -- },
  git = {
    enable = true,
    ignore = false,
  },
  renderer = {
    group_empty = false,
    --   root_folder_label = true,
    highlight_git = true,
    --   -- highlight_opened_files = "none",
    --
    indent_markers = {
      enable = true,
    },

    icons = {
      show = {
        file = true,
        folder = true,
        folder_arrow = true,
        git = true,
      },
    },
  },
}
